# 关于这个 vault / 用户背景

## 用户
- **Henry**，正在系统重学 C 语言，最终目标是 **408 数据结构**（链表/树/图）。
- **C 基础来源**：大一上学期学的，当时**只学到「字符串」（第 7 章）就停了**，后续（动态内存、指针深入、链表）完全没学过。
- **学习习惯（需注意）**：容易"流畅的错觉"——快速过完一遍觉得"大概懂了、想起来了"，但**一个题没做**。重学时反复在基础上卡壳（`int *p` 的含义、`.` vs `->`、浅/深拷贝、`dst/src` 参数、二维退化）。
  → 对策：**多要求他合上教材默写 + 实际跑代码**，别让他纯读。指针、字符串两章是重中之重，要重点盯。

## 教材位置
- 路径：`C语言速成教材/`，共 13 章。
- 当前进度：**1-7 章已快速过完并且完成了** `C语言速成教材/自测题-基础篇1-7章.md` **，并且已经完成了 08 09章节的学习**。
- ⚠ 建议：进 08 前先把 5（指针）、7（字符串）的题做扎实，否则雪崩。

## 协作偏好
- 讲解用**类比 + 内存图 + 对照表**，效果最好（他多次靠类比打通，如 bash `cat` ↔ `strcat`）。
- 回答用中文。
- 文件引用用 wikilink。
- 严禁直接为用户给出代码，一定要先启发讲解思路，而不是直接替用户学习
- 不要说"命根子"这个词（用"重中之重""核心根基"等替代）。
- 看到用户可能没按照约定协作，先尝试找到用户代码，并查看用户写的代码，而不是过早的询问用户
